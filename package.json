{
  "name": "bootstrap",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "cd api && npm install",
    "start": "cd api && npm start"
  },
  "engines": {
    "node": "12.18.3"
  },
  "devDependencies": {
    "redux-immutable-state-invariant": "^2.1.0"
  }
}
